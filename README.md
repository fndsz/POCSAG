# POCSAG 控制端改动说明（V2.1-4M-5CH）

基于 fndsz/POCSAG 仓库 `ESP8266/` 版本修改。STM32 端固件**不用动**，串口协议完全兼容。

---

## 一、界面美化

四个页面（单呼 / 群呼 / Wifi / 升级）统一换了视觉：

- **渐变品牌头**：顶部蓝色渐变条，替代原来的纯文字标题，含 LOGO、页面标签、深浅色切换按钮
- **卡片**：大圆角 + 柔和阴影，标题带渐变竖条装饰
- **表单**：输入框加圆角与聚焦光晕，下拉框换成自绘箭头
- **开关**：所有复选框改为滑块开关（群呼勾选、天气勾选、自适应间隔、同频归组等）
- **底栏**：圆角上浮 + 阴影，手机上不用滚到底也能发
- **其他**：进度条、提示条、热点列表同步改圆角；热点信号用四格柱状图显示
- 深浅色双主题跟随系统，手动切换后记忆到浏览器

样式是作为一层"皮肤"叠加在原布局上的，所有按钮、输入框、JS 逻辑都没动。

## 二、选择按钮优化（群呼列表）

### 先修了一个会让页面"点了没反应"的 bug

上一版把群呼从 10 路改成 5 路时，漏改了 `refresh()` 里的一个循环（还写着 `j<10`）。
结果：循环跑到第 6 行时 `boxes("sel")[5]` 是 `undefined`，读 `.checked` 直接抛异常，
**整个 `refresh()` 崩掉** —— 表现为勾选后"已选 N 行"不更新、耗时预估一直是"预计 —"、
参数校验提示也不弹。这版已修，并把路数统一成 `var N=5`，以后改路数只改一处。

### 按钮本身

之前列表里的勾选框被压成了 **22px 的小圆点**（旧样式 `.c-chk input[type=checkbox]`
的选择器比通用规则更"专"，把 44×26 的开关覆盖掉了）。现在按用途分成两类：

| 位置 | 控件 | 说明 |
|---|---|---|
| 群呼列表「发送」「天气」 | **26px 大号勾选框**（带对勾） | 表格里勾选语义明确，比滑块直观 |
| 「自适应间隔」「同频归组」「推送时间点」「记住密码」 | **44×26 滑块开关** | 设置项开关，滑块语义更对 |

（后面这些滑块之前同样被压成了 20px 小圆点，一并修好了。）

### 交互

- **整行可点**：点行内任意空白处即可切换该行「发送」勾选，手机上不用去戳 26px 的小框
  - 点输入框 / 下拉框 → 正常编辑，不触发勾选
  - 点「天气」那一格 → 不影响「发送」（两者互相独立）
- **选中行整行高亮**：手机上背景变蓝 + 蓝框；宽屏下左侧竖条 + 淡渐变
- **序号徽章随状态变色**：未选灰底灰字，选中蓝底白字 —— 哪几行会被发出去，扫一眼就知道

## 三、Logo 换成小猪线稿 + 「发送」列修正

### 顶部 Logo：PO → 小猪线稿

原来是个写着 "PO" 两个字的方块，换成了**你给的那只小猪线稿**。

做法：对参考图做**自动描图**，而不是手敲贝塞尔坐标——

1. 按绿色通道把线条从黑底里抠出来，得到二值掩膜
2. 提取轮廓 + 道格拉斯-普克算法简化成 268 个点的多边形
3. 等比居中归一化到 `viewBox="0 0 100 100"`，白色填充 + `fill-rule="evenodd"`

**线宽按你要求调细了**（这一版的改动）：

上一版为了让 32px 下看得清，先把线条**膨胀加粗**再描轮廓，结果明显比原图胖。
这版改成**保持原图线不动，只补 0.5 单位的白色描边**来提亮——
两者区别很大：膨胀会把形状往外"顶"变形（尤其尖角和尾巴），描边是沿法向均匀外扩，保形好得多。

| 方案 | IoU（形状吻合度） | 粗细占比 | 32px 对比度 |
|---|---|---|---|
| 原图参考 | — | 0.079 | — |
| 上一版（膨胀加粗） | 0.497 | 0.156（比原图胖一倍） | 23.2 |
| **这一版（原线 + 0.5 描边）** | **0.750** | **0.083（≈原图）** | 13.5 |

形状吻合度 **+51%**，粗细几乎回到原图水平（0.083 vs 0.079）。
代价是 32px 下淡一些（对比度 23.2 → 13.5），但轮廓、耳朵、四条腿、卷尾都清晰可辨，
观感就是原图那只纤细的手绘猪。

| 项 | 值 |
|---|---|
| 体积 | 单页内联 3.1KB，gzip 后约 1.2KB |
| 依赖 | **纯内联 SVG，不引外部图片**，离线/内网热点照样显示 |

四个页面（单呼 / 群呼 / Wifi / 升级）统一替换，图标尺寸 32px。

> 尾巴的螺旋圈距太近，之前加粗那版几圈会糊成小团；
> 这版线细了，卷尾的螺旋结构反而能看出来了。

### 「发送」列被挤歪 —— 顺手修的真 bug

宽屏表格里，「发送」那一格是歪的，根因是个漏网的选择器：

| 格子 | 标签写法 | 宽屏是否被隐藏 |
|---|---|---|
| 天气 | `<i class="lb">天气推送</i>` | ✅ 被 `.row .lb{display:none}` 藏掉 |
| **发送** | `<span>发送</span>` | ❌ **不是 `.lb`，没被藏掉** |

于是 `26px 复选框 + 6px 间距 + 28px 文字 = 60px` 硬塞进 52px 的列里，就挤歪了。
而天气列因为用的是 `.lb` 反倒一直正常 —— 看起来像是"只有发送列有问题"。

修法：**宽屏下隐藏行内的"发送"二字，复选框居中**（和天气列一致）。
表头本来就写着"发送"，行内再挂一遍纯属重复；手机上没有表头，文字保留。

顺带把第一列从 52px 收到 46px（藏掉文字后不需要那么宽），多出来的空间给频率和地址码列。

## 三·四、切换按钮点不动的真正原因（装饰圆挡住了）+ 按钮改小

### 根因：不是脚本坏了，是点击被装饰圆吞了

品牌头右上那两个半透明圆是**纯装饰**（`.brand::after` / `.brand::before`），
但它们正好盖在主题切换按钮上面：

| 层 | 位置 | 是否吃点击 |
|---|---|---|
| `::after` 装饰圆 | 140×140，圆心距按钮中心 **15px**（半径 70） | 改前：**吃** ← 按钮整个被罩住 |
| 主题按钮 | 30×30，品牌头最右侧 | 被上面那层挡住 |

按 CSS 绘制顺序，`::after` 是「最后一个子元素」，会盖在按钮**之上**；
两者都是定位元素且 `z-index:auto`，于是装饰层赢 ——
**按钮看得见、点得着位置，但点击全被它吃掉，没有任何反应**。

几何验证（品牌头高 72px、按钮居中于右侧）：

```
改小前 38px：按钮中心距装饰圆心 15.0px < 半径 70  → 完全被覆盖
改小后 30px：按钮中心距装饰圆心 17.7px < 半径 70  → 仍被覆盖（所以必须修层级）
```

修复两处：
- 装饰层加 `pointer-events:none` —— 纯装饰永不拦截点击
- 按钮加 `z-index:3` —— 压在装饰层之上，双保险

### 按钮改小

`38×38 / 字号 17px` → **`30×30 / 字号 15px`**，圆角圆形保持。
另外加了 `touch-action:manipulation`，去掉移动端约 300ms 的点击延迟，
点起来更跟手。

## 三·五、修复日夜（深浅色）切换失效

### 根因：群呼页的切换按钮是「隐身」的

我上次给四个页面加品牌头时，把主题按钮统一挪进了顶部渐变条，并加了
`.card h2 .tgl,.sum .tgl{display:none}` 把旧位置的按钮藏起来。

**群呼页漏了这一步** —— 它的按钮还留在底部「已选 N 行」那条 `.sum` 状态栏里，
于是被上面那条 CSS 直接 `display:none` 掉了，而品牌头里又没补上。
结果就是：群呼页**根本看不到切换按钮**，点了自然也没反应。

| 页面 | 按钮位置 | 可见 |
|---|---|---|
| 单呼 / Wifi / 升级 | 品牌头 | ✅ |
| **群呼（主页面）** | `.sum` 状态栏（被 CSS 隐藏） | ❌ |

已把按钮移进群呼页品牌头，实测四个页面全部可见。

### 顺带做的加固

原实现有个隐患：直接写裸的 `matchMedia(...)`，一旦浏览器不支持就是
`ReferenceError` —— 而这个异常发生在真正切换之前，**点了完全没反应、也不报错**，
排查起来非常难受。同理 `$("theme").onclick` 在按钮缺失时会直接把整页脚本打死。

现在每个可能出问题的调用都单独兜住：按钮缺失不影响其它功能，
没有 `matchMedia` 也能正常切。

### 改成三态，并且看得见当前状态

原来只有「浅/深」两态，一旦手动选过就再也回不到「跟随系统」，
按钮图标也永远是 ◐，看不出当前到底是哪一档。现在：

| 图标 | 状态 | 说明 |
|---|---|---|
| ◐ | **跟随系统** | 不设 `data-theme`，系统切深就深、切浅就浅 |
| ☀ | 浅色 | 手动锁定 |
| ☾ | 深色 | 手动锁定 |

点击循环：跟随系统 → 浅色 → 深色 → 跟随系统。
同时会同步更新 `<meta name="theme-color">`，手机浏览器的地址栏颜色也跟着变，
一眼就能确认切生效了。选择记在浏览器本地，刷新和换页都保持。

## 三·六、新增「追码 / 追频」独立页面

路由 `/hunt`（`hunt.html`），四个页面的底部导航都加了入口。

用途：手里有台呼机但不知道地址码和频点时，地毯式遍历 ——
每个频点上把地址码区间挨个发一遍简短测试消息，
**呼机响了就把当时的「频率 + 地址码」记下来**，那一组就是它的。

### 页面结构

| 卡片 | 内容 |
|---|---|
| 扫描范围 | 频率起止 + 步进、地址码起止 |
| 发射参数 | 速率、相位、响声、机型、测试消息、间隔 |
| 执行 | 汇总（总条数 + 预计耗时）、进度条、当前频点/地址码、开始/停止 |

独立成页后就不能再蹭群呼页的「消息与全局参数」了，所以把**响声 / 机型 /
自适应间隔**也搬了过来。这些值提交时会写回 `cfg` 并落盘，两页共用同一套。

### 两个关键设计

**1）遍历顺序：频率在外、地址在内**

`#SET+FREQ` 要等锁相环重新锁定，比换地址码贵得多：

| 顺序 | 切频次数（41 频点 × 100 地址） |
|---|---|
| **频率外层（本实现）** | **41 次** |
| 地址外层 | 4100 次（慢 99%） |

**2）不预生成任务**

几千上万条塞不进 `TX_QUEUE(12)`，而且这是「边扫边看」的活，随时要能停。
只在队列快空时补货（保持 3 条），停止最多再跑 3 条（约 6 秒）。

### 关于「当前地址码」的准确性（这里踩过一个坑）

最初用扫描游标反推当前位置，但 `huntTick` 会预先往队列里填几条，
游标已经跑到**真正正在发射那条的前面**（最多差一个队列深度）。
照它显示，用户看到的号码会比呼机实际响的那条**超前十几条** —— 那这功能就废了。

改成直接读队列头 `txq[qHead]`，它就是此刻正在发射（或正要发射）的那条，**精确**。

### 进页面不能顺手停扫描

早期版本为了「探一下设备是不是在扫」，进页面直接发了 `stop=1` ——
结果一进页面就把正在跑的扫描掐了，等于白扫。
现在改成**只查状态**：若发现设备在扫，直接把进度接上显示。

### 耗时心里有数

一条约 1~2.5 秒（1200bps、短消息）：

| 范围 | 条数 | 预计耗时 |
|---|---|---|
| 3 频点 × 100 地址 | 300 | **约 10 分钟** |
| 单频点 × 1000 地址 | 1000 | **约 33 分钟** |

**建议先单频点扫地址码**（频率起止填同一个值），扫到再单独扫频率，比二维地毯式快得多。

### 其它

- 频率一律用 **0.0001 MHz 为单位的整数**参与运算，避免浮点累加漂移
  （0.0250 连加几十次会出现 152.8499 这种脏值）
- 参数不合法时**「开始扫描」按钮直接置灰**，不让你点了才报错
- 超过 5000 条拒绝启动；超过 300 条弹确认
- 「停止扫描」和底部 dock 都能停
- `POST /hunt` 开始或停止，`GET /hunt` 出页面（`/scan` 已被 Wifi 扫描占用）

## 三·七、天气推送独立成页

路由 `/weather`（`weather.html`）。群呼页只管发消息，天气单独一页。

| 页面 | 路由 | 内容 |
|---|---|---|
| 单呼 | `/` | 一次一频一址 |
| 群呼 | `/group_call` | 5 路列表 + 消息与全局参数 |
| **天气推送** | **`/weather`** | 数据源、城市、定时、推送目标、预览 |
| 追码 / 追频 | `/hunt` | 扫描定位未知呼机 |
| Wifi | `/wifi` | 配网 |

六个页面底部导航互链，来回切不用退回首页。

### 关键：推送目标不能"看不见"

天气推送的目标 = 群呼列表里「天气」列的勾选。独立成页后**这一列就看不到了**，
如果不管，用户根本无从设置要发给谁 —— 这个功能等于废了一半。

所以天气页加了「推送目标」卡片：列出 5 路的频率 + 地址码（方便确认发给谁），
每行一个勾选框，就是 `cfg.pg[i].wx` 本身，**两页共用同一份设置，在哪边改都一样**。

配套改了固件：`/wxcfg` 现在也接受 `w0..w4`。只有带这些参数过来才更新 ——
群呼页的保存不带它们，所以那边仍以列表为准，两边互不干扰。

### 群呼页的连带清理（这里有个会致命的坑）

天气卡片从群呼页移走后，页面里那批 JS（`wxSave` / `srcHint` / `syncSlots` /
`HINTS` / `loadCfg` 里的 `c.wx` 块…）**引用的是已经被删掉的元素**。

不清理的话，`loadCfg()` 执行到 `$("wsrc").value = ...` 时 `$("wsrc")` 是 `null`，
直接抛 `TypeError` —— **整个配置加载中断**，5 路频率地址全读不出来，
群呼页等于报废，而且报错藏在 `fetch` 回调里，很难一眼看出。

已经把这些残留全部清掉，并做了校验：JS 里 `$("xxx")` 引用的 id 在页面中
**全部存在**（无悬空引用）。

## 三·八、修复编译错误：'hunt' was not declared in this scope

实机编译时报 `'hunt' was not declared in this scope`，根因是**声明顺序**：

| 位置 | 内容 |
|---|---|
| 行 716（`txPump`） | 用 `hunt.total` 给扫描单独记进度 |
| 行 799 | `struct Hunt { ... } hunt;` ← 声明在这里 |

C++ 要求先声明后使用，`txPump` 在 `hunt` 之前，所以编译不过。
已把 `HUNT_*` 宏与 `struct Hunt hunt` 整体前移到发送队列声明之后（行 131~148），
排在 `txPump`（行 704）之前。

顺带清掉一个警告：

```
warning: unused variable 'ok' [-Wunused-variable]
```

`bool ok = WiFi.softAP(...)` 只在 `DBG(...)` 里用到，而 `DEBUG=0` 时
`DBG` 展开成 `do {} while(0)`，`ok` 就没人用了。补 `(void)ok;` 显式吃掉。

另外加固了一处隐患：`argVal("type")[0]` 这类写法 — Arduino 的
`String::operator[]` **不做越界检查**，参数被显式提交成空串时会读到脏值。
改成先判长度再取首字符。

## 三·九、品牌名改「寻呼机发射器」+ 日夜切换标志调小

### 1）品牌名

`摩托罗拉寻呼发射器` → **`寻呼机发射器`**，六个页面统一，浏览器标签标题同步。

顺手修了两个页面：**追码页和天气页的 `<title>` 副标题是照抄群呼页的，都写着「群呼」**，
现在改成各自页面名（`追码 / 追频`、`天气推送`）。

从 9 个字缩到 6 个字还顺带解决一个隐患：原来在窄屏上标题会触发 `text-overflow:ellipsis`
被截断，现在完整显示了。

| 页面 | title |
|---|---|
| 单呼 | 寻呼机发射器 · 单呼 |
| 群呼 | 寻呼机发射器 · 群呼 |
| 天气推送 | 寻呼机发射器 · 天气推送 |
| 追码 / 追频 | 寻呼机发射器 · 追码 / 追频 |
| 配置 Wifi | 寻呼机发射器 · 配置 Wifi |
| 固件升级 | 寻呼机发射器 · 固件升级 |

### 2）日夜切换按钮

`30×30 / 字号 15px` → **`26×26 / 字号 12px`**，边框和背景透明度也调淡了一点
（更融入品牌头的渐变，不再抢主标题的视觉重量）。

**这里补了个必要的处理**：按钮一缩，点击热区也跟着只剩 26px，手机上容易点空。
用伪元素把热区撑到 **40×40**，视觉尺寸不变：

```css
.brand .tgl::before{content:"";position:absolute;left:50%;top:50%;
  width:40px;height:40px;transform:translate(-50%,-50%)}
```

（装饰圆那条 `pointer-events:none` 和按钮的 `z-index:3` 都还在，
上次修的「点不动」问题不会复发。）

## 三·十、去掉日夜切换按钮的外圈

按钮改成**只剩图标本身**（◐ / ☀ / ☾），去掉那层圆形边框和半透明底：

```css
background:none;border:0;          /* 原来是 border-radius:50% + 半透明白底 + 白边 */
```

配套两处微调：

- **字号 12px → 14px**：没有圈了，图标是唯一的视觉主体，太小会看不清
- **加 `text-shadow`**：去掉白底后，白色图标直接压在品牌头的蓝紫渐变上，
  浅色区域容易糊掉，靠阴影保证任何位置都看得清

**点击热区（40×40 伪元素）保留** —— 去掉外圈后按钮视觉上更小了，热区更不能撤。

注意：`.logo`（猪徽章）和品牌头的两个装饰圆**仍是圆形的**，这次只动了
`.brand .tgl`，没有误伤。

## 三·十一、页面名标签也去掉外圈

品牌头右侧的页面名（单呼 / 群呼 / 天气推送 / 追码 / 追频 / Wifi / 升级）
原来是「胶囊」样式 —— 圆角 999px + 半透明白底 + 白边，和切换按钮一样带圈。
现在统一去掉，只留文字：

```css
background:none;border:0;
```

配套微调：

- **加字重 600 + 字距 0.5px**：没有底衬托了，纯文字要自己立得住
- **加 `text-shadow`**：和切换按钮同一套处理，保证压在蓝紫渐变上仍清晰
- **内边距改为 `0 0 0 10px`**：原来靠左右各 11px 撑开胶囊，现在只需与
  主标题保持间距，右侧交给切换按钮

六个页面已确认全部生效，切换按钮（上一轮）也仍是无外圈状态。

## 四、群呼列表的进一步界面优化

### 底部按钮分组（5 个挤一行 → 3 组）

原来「全选发送 / 全不选 / 反选 / 全选天气 / 清空天气」5 个按钮挤在一行，
手机上每个只剩几十像素宽，很容易点错。改成三组：

| 组 | 按钮 |
|---|---|
| 发送 | 全选 · 全不选 · 反选 |
| 天气 | 全选 · 清空 |
| 批量 | 同步首行 · 地址递增 |

手机上每组占一行、组名用左侧小标签点明；宽屏时三组横向排开。

### 新增两个批量工具（实用）

配置 5 路同频时，频率/速率/相位整批一样，只有地址码各不相同 ——
正好对应这两件事，一次填完，不用一行行手敲：

- **同步首行**：把 01 行的频率 / 速率 / 相位刷到其余各行（**地址码不动**）
- **地址递增**：按 01 行的地址码往后 +1，自动补足前导 0（`0000001` → `0000005`）

两个都有保护：01 行频率为空会提示先填；地址码位数不够装不下递增结果时
会明确告诉你位数不够（而不是悄悄截断出一个错号码）。

### 其他细节

- **表头吸顶**：往下滚时列名一直挂着，不用滚回顶部才知道每列是什么
- **桌面端悬停高亮**：鼠标移上去有淡蓝反馈。用 `@media (hover:hover)` 限定，
  触屏上不会出现"点一下 hover 粘住"的假选中
- **天气勾选框与「发送」统一**：都是 26px 圆角方框 + 对勾，不再是一个方一个圆

## 五、字体与数字排版优化

### 数字全部改成等宽

频率 `152.8250`、地址码 `0000002` 这类数字用比例字体有两个毛病：小数点对不齐，
几行扫下来得逐位比对；改数值时宽度跳变，整行会抖。

换成等宽字体 + `tabular-nums` 之后，位数相同就严格对齐，哪行填错了扫一眼就能看出来。
应用范围：频率、地址码、速率、序号徽章、「已选 N 行」统计、耗时预估、字节数。

**相位没用等宽** —— 它的选项是「正相位/负相位」中文，等宽字体对汉字没意义，反而别扭。

### 字体栈补全

原字体栈只有 `PingFang SC` / `Microsoft YaHei` / `Heiti SC`，在鸿蒙、部分 Linux 桌面
和没装雅黑的 Windows 上会掉到默认字体，观感不一致。补全为：

```
-apple-system, BlinkMacSystemFont, "PingFang SC", "HarmonyOS Sans SC",
"Source Han Sans SC", "Noto Sans CJK SC", "Microsoft YaHei", "Heiti SC", sans-serif
```

四个页面统一。

### 字重与字间距

- 表头（`发送 / 序号 / 频率(MHz) / 地址码 / 速率 / 相位 / 天气`）加字重 600 和 0.4px 字距，
  跟下面的单元格拉开层次，不再像一行灰色小字
- 「全选发送 / 全不选 / 反选」这类次要按钮补 0.5px 字距
- 卡片标题、行内标签的汉字补 0.2px 字距，密排时明显更清楚

## 六、群呼改为 5 路

- 网页群呼表格从 10 行减到 **5 行**，卡片标题标注"共 5 路"
- 固件 `#define PAGER_NUM 5`，参数读取、发送排队、同频归组逻辑同步跟着变
- 配置文件格式升到 `ver=3`，旧的 10 行配置读进来只会保留前 5 行

想改回别的路数：改 `ESP8266.ino` 里的 `PAGER_NUM`，同时改 `原始html/group_call.html`
里的行数（复制/删除 `.row` 块），再跑一次 `python3 tools/html2c.py` 重新生成 `html.c`。

## 七、天气改为腾讯源（免费免 Key）

| 项目 | 原来 | 现在 |
|---|---|---|
| 默认源 | Open-Meteo（境外） | **腾讯天气 wis.qq.com（国内，免 Key）** |
| 备选源 | 心知天气 / 和风（均需 Key） | Open-Meteo（免 Key） |
| API Key 输入框 | 有 | **已删除**，两个源都不需要 |
| 位置填写 | 经纬度 `22.8170,108.3665` | **行政区划** `广西,南宁,青秀区` |

- 默认位置填的是广西南宁，城市名默认"南宁"
- 新增**当日最高/最低温**，会一起发到呼机：`南宁 多云 28℃ 26~33℃ 湿度65% 东南风2级`
- 旧配置里存的是经纬度时，升级后会自动识别并换回默认行政区划，不会一升级就取不到天气
- 汉字转码表补了几个腾讯会用到的词（霾、雨凇、霜冻、热带风暴、强热带风暴），避免缺字

## 八、隐藏固件升级页

按你的要求把升级页藏起来了，做了三层，不是只删个链接：

1. 三个页面（单呼 / 群呼 / Wifi）底部导航里的「固件升级」入口**已删除**
2. `/webupdate` 和 `/update` 路由注册成 **404**（不是 302 跳首页 —— 跳转等于告诉对方这个地址存在）
3. 固件顶部加了总开关，想恢复时改一行即可：

```c
#define ENABLE_OTA 0    // 0=隐藏（当前）  1=恢复升级页
```

改成 1 重新编译烧一次，升级页和 OTA 就都回来了（页面数据一直留在 Flash 里，约 8KB，
所以随时能恢复，不用重新生成 `html.c`）。

> 注：升级页隐藏后仍留在代码里，别人即使猜到 `/webupdate` 也只会拿到 404。

## 九、Wifi 搜不到热点的排查（换过 Flash 的模块必看）

**你这个情况（自己换过 4MB Flash + 手机完全搜不到热点），大概率是 RF 校准数据丢了。**

### 原因

ESP8266 的射频校准值存在 Flash 倒数第 4 个扇区：

| 容量 | rfcal 地址 |
|---|---|
| 1MB | `0xFC000` |
| **4MB** | **`0x3FC000`** |
| 8MB | `0x7FC000` |

新 Flash 芯片出厂整片是 `0xFF`，而平时烧程序只写程序段，**不会碰这一块**。
phy 初始化读不到校准值 → 射频基本不工作 → **手机完全搜不到热点**，
或者信号弱到贴脸才能连，STA 也扫不到任何网络。

### 怎么办

```bash
pip install esptool

# 第一步：只读体检，不动 Flash，随便跑
python3 tools/fix_rfcal.py --port COM3          # Linux 用 /dev/ttyUSB0

# 脚本报告"rfcal 是空的"才加 --write 修复（会先自动备份）
python3 tools/fix_rfcal.py --port COM3 --write
```

写的是 **默认值**，能让 Wifi 恢复正常工作，但信号强度不如芯片原厂校准的那一版。
如果换 Flash 前按 README 备份过 `rfcal_backup.bin`，用 `--default-bin` 指回去更好。

### 固件里已经加的自检

开机时会自动读 rfcal 区，发现全是 `0xFF` 就通过调试口喊出来（含具体地址和修复命令）。
地址按 `真实容量 - 0x4000` 自动算，1MB/4MB/8MB 都准，不用手改。

另外 AP 启动**固定到信道 1**（2.4G 里全球通用）—— 以前让 SDK 自己挑，
可能挑到 12/13，部分手机不会去扫这两个信道，表现同样是"搜不到热点"。

### 看日志

`ESP8266.ino` 顶部 `#define DEBUG 0` 改成 `1`，重新烧一次。
日志走 **Serial1（TX = GPIO2）**，把模块 GPIO2 接 USB 转串口的 RX（GND 共地），
115200 打开就能看。不会占用与 STM32 通讯的那一路串口。
（原版 DBG 从没 `begin()` 过，等于调试信息全丢，这版补上了。）

### 还是不行再看这些

1. **Flash Size 选错** —— 必须是 `4MB (FS:2MB OTA:~1019KB)`，选 1MB 直接不启动
2. **供电** —— ESP8266 发射瞬间电流大，USB 转串口 3.3V 往往带不动，会反复重启；
   建议独立 3.3V ≥ 500mA
3. **天线 / 假负载** —— 没接天线就别让它发射，会烧 RF 功放（但搜热点不受影响）
4. **GPIO2 不能拉低** —— 上电时 GPIO2 必须为高，拉低会进下载模式

## 十、Flash 4MB

本固件（程序 + 4 个页面数据 + LittleFS）需要 4MB 布局，Arduino 里必须选：

```
Board:      Generic ESP8266 Module
Flash Size: 4MB (FS:2MB OTA:~1019KB)
Flash Mode: QIO 或 DIO 均可
SSL Support: All SSL ciphers   ← 天气是 HTTPS，必须开
```

选 1MB 会编译不过。开机时实际 Flash 容量会打到调试口（`DEBUG` 置 1 可见）。

---

## 文件清单

```
ESP8266/
├── ESP8266.ino        ← 固件主文件（已改）
├── html.c             ← 页面数据（已重新生成）
├── html.h             ← 页面声明
└── weather_gbk.h      ← 汉字转码表（已补词）
原始html/              ← 网页 UTF-8 源文件（已美化）
├── index.html  group_call.html  wifi_page.html  update.html
tools/
├── html2c.py          ← 网页 → html.c 生成器
└── fix_rfcal.py       ← Wifi 搜不到热点的体检 / 修复脚本
```

改网页的流程：改 `原始html/` 里的文件 → 跑 `python3 tools/html2c.py` → 重新编译烧录。

## 烧录

1. Arduino 打开 `ESP8266/ESP8266.ino`（四个文件要在同一目录）
2. 按上面表格设好 Flash Size
3. 首次建议串口烧录；之后可用网页"固件升级"页 OTA
4. 上电后连 `POCSAG` 热点，浏览器开 `http://192.168.4.1`

---

## 天气排障（重要）

**腾讯天气接口本次没能实测连通**：开发环境访问所有外网接口都被网关拦截
（Open-Meteo 同样返回 403），所以只能做代码级校验，实际能否取到请以设备为准。

如果天气取不到，按这个顺序排查：

1. 群呼页顶部看连接状态——必须**先连上能上网的路由器**（热点本身不能上网）
2. 点"获取天气预览"，看返回的具体错误提示
3. 行政区划名称要和官方一致，如 `广西,南宁,青秀区`；区县可留空写成 `广西,南宁`
4. 腾讯是 HTTPS，`SSL Support` 必须选 All SSL ciphers
5. 腾讯接口万一调整/失效，在"数据源"下拉切到 **Open-Meteo**，位置改填经纬度
   `22.8170,108.3665`（注意：切过去后城市名要手动填，它不回传）

这类免 Key 公开接口不保证长期稳定，代码里做了字段级容错：某个字段解析不到就跳过，
不会因为接口小改动导致整条天气推不出去。

---

## 其他说明

- 发射前务必接好天线或假负载，否则会烧毁 RF 功放
- 数字机只认 `0-9` 与 `A-F`，天气会自动切换成纯数字格式（不含高低温和城市名）
- 本项目遵循 GPL，个人 DIY 免费，商用需授权
